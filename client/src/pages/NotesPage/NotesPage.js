import React, { Fragment } from "react";

import Actions from '../../components/Actions';

const NotesPage = () => {
	return (
    <Fragment>
      <Actions />
    </Fragment>
  )
};

export default NotesPage;
